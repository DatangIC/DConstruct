<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="checkRepairItem"
            type="com.datangic.smartlock.components.CheckRepairItem" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_4dp">

        <androidx.constraintlayout.utils.widget.ImageFilterView
            android:id="@+id/item_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_2dp"
            android:src="@{checkRepairItem.itemIcon}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_keyboard"
            app:tint="@color/grey_600" />


        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/item_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_2dp"
            android:gravity="center"
            android:hint="@string/management_fingerprint"
            android:text="@{checkRepairItem.itemName}"
            android:textAppearance="@style/AppTheme.TextAppearance.Subtitle1"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@id/item_icon" />

        <androidx.constraintlayout.utils.widget.ImageFilterView
            android:id="@+id/item_flag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_2dp"
            android:src="@{checkRepairItem.correct==true? @drawable/ic_correct:@drawable/ic_warning}"
            android:tint="@{checkRepairItem.correct==true? @color/green:@color/red}"
            android:visibility="@{checkRepairItem.correct == null? View.GONE : View.VISIBLE}"
            app:layout_constraintLeft_toLeftOf="@id/item_icon"
            app:layout_constraintRight_toRightOf="@id/item_icon"
            app:layout_constraintTop_toBottomOf="@id/item_name" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>