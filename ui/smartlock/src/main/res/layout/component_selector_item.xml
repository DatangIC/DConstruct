<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>


        <variable
            name="onItemClick"
            type="android.view.View.OnClickListener" />

        <variable
            name="selectorItem"
            type="com.datangic.smartlock.components.SelectorItem" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="?actionBarSize"
        android:background="@drawable/selector_grey"
        android:clickable="true"
        android:focusable="true"
        android:onClick="@{onItemClick}"
        android:paddingTop="@dimen/margin_4dp">


        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/selector_name"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center"
            android:hint="@string/title_setting"
            android:text="@{selectorItem.itemName}"
            android:textAppearance="@style/AppTheme.TextAppearance.Subtitle2"
            android:textColor="@{(selectorItem.selected? @color/color_on_surface : @color/text_primary)}"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />


        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:background="@color/divider"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintRight_toRightOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>